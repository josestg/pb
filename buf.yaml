version: v2
modules:
  - path: src
lint:
  use:
    - STANDARD
  except:
    - ENUM_VALUE_PREFIX
    - ENUM_ZERO_VALUE_SUFFIX
breaking:
  use:
    - FILE
deps:
  - buf.build/bufbuild/protovalidate
  - buf.build/googleapis/googleapis